second generate description when submitted  and show the updated code with changes made on function names and all variables in the given input code 

after user click on verified of this description and given input code with all changes done to it wrt to description -> we need to generate the title, difficulty, all four languages codes (the UI should like showing all four codes side by side and below there should be input and output box with diffchecker implemented to check outputs across all four languages 

if any code gives an error, we should have an error/warnings box as well 

current you can use JudgeAPI to test it and later i will provide a API for it and share the response of it as well