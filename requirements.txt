# Pinned Python environment for the CP-prep pipeline.
#
# This is a full, transitive lock of pipeline/requirements.txt (the loose
# top-level spec that Dockerfile installs). Generated with:
#
#   python3 -m venv .venv
#   .venv/bin/pip install -r pipeline/requirements.txt
#   .venv/bin/pip freeze --exclude-editable > requirements.txt
#
# To reproduce the exact local env:
#
#   .venv/bin/pip install -r requirements.txt
#
# Point the app at the venv with PYTHON_PATH=<repo>/.venv/bin/python
# (see src/lib/cp-prep/index.ts). Resolved on Python 3.11 / macOS arm64;
# regenerate after changing pipeline/requirements.txt.

annotated-types==0.8.0
anthropic==1.4.0
anyio==4.15.1
boto3==1.43.89
botocore==1.43.89
certifi==2026.7.22
charset-normalizer==3.5.1
docstring_parser==0.18.0
h11==0.16.0
httpcore==1.0.9
httpcore2==2.12.0
httpx==0.28.1
httpx2==2.12.0
idna==3.19
jiter==0.16.0
jmespath==1.1.0
numpy==2.4.6
openai==3.8.0
pydantic==2.13.5
pydantic_core==2.46.5
python-dateutil==2.9.0.post0
regex==2026.9.3
requests==2.34.2
s3transfer==0.19.2
six==1.17.0
sniffio==1.3.1
tiktoken==0.14.0
truststore==0.10.4
typing-inspection==0.4.4
typing_extensions==4.16.0
urllib3==2.7.0
