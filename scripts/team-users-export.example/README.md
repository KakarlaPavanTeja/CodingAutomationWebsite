# Team users export

Team lead: run `npm run export:team-users` on Replit, zip `scripts/team-users-export/`, share with team.

Team members: extract to `scripts/team-users-export/` before running setup.
